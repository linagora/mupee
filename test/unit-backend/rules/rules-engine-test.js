'use strict';

var chai = require('chai');

var expect = chai.expect;

describe('The Rules Engine module', function() {

  it('should add default rules in storage', function(done) {
    expect(1).to.equal(1);
    done();
  });

});